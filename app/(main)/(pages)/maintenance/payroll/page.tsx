import PayrollTabs from "@/Features/maintenance/Components/PayrollTabs";

const Payroll = () => {
    return (
        <div className="card">
            <PayrollTabs />
        </div>
    );
};

export default Payroll;
